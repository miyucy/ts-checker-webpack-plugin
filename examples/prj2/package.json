{
  "name": "prj2",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "node --experimental-worker node_modules/.bin/webpack --run-dev --reporter basic --config webpack.config.js --log-level trace",
    "watch": "node --experimental-worker node_modules/.bin/webpack-serve --config webpack.config.js --log-level trace --log-time",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-loader": "^5.2.2",
    "typescript": "^3.1.3",
    "webpack": "^4.22.0",
    "webpack-command": "^0.4.1",
    "webpack-serve": "^2.0.2"
  }
}
